<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestProject.Views.AboutPage"
             Title="{Binding Title}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout>
        <ScrollView>
            <StackLayout Padding="20">
                <Button Text="Sélectionner une image" Clicked="OnSelectImageClicked" />
                <Image x:Name="imageView" />
                <Button Text="Compresser l'image" Clicked="OnCompressImageClicked" />
                <Image x:Name="imageCompressedView"  />
            </StackLayout>
        </ScrollView>
    </StackLayout>
</ContentPage>
